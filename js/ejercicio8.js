// Escribe un programa que pida un número y diga si es divisible por 2 

let numero = parseInt(prompt('escriba'));

if(numero % 2 == 0 ){
    document.write('Divisible');
}else{
        document.write('no divisible' );
    }
